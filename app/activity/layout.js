export const metadata = {
  title: "GitHub Activity — Aarav Goel",
  description: "Live contribution calendar and push activity graph for coderaarav12.",
};

export default function ActivityLayout({ children }) {
  return children;
}
